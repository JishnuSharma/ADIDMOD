export enum DeviceType {
  Temperature = 'Temperature',
  Humidity = 'Humidity',
  Smoke = 'Smoke',
  Motion = 'Motion',
  Infrared = 'Infrared',
  Pressure = 'Pressure',
}

export enum FileType {
  Excel = 'excel',
  CSV = 'csv',
}
